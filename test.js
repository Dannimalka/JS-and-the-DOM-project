/*not used in the actual code*/
/*not used in the actual code*/
/*not used in the actual code*/

/*
const colorchanger = document.querySelector("nav");
navbar.addEventListener("click", function () {
  console.log("this worked! clicked");
  navbar.style.backgroundColor = "pink";
*/
/*
function onScreen(section) {}
*/

/*
const body = document.body;
let lastScroll = 0;

window.addEventListener("scroll", () => {
  const currentScroll = window.pageYOffset;
  if (currentScroll <= 0) {
    body.classList.remove("scroll-up");
    return;
  }

  if (currentScroll > lastScroll && !body.classList.contains("scroll-down")) {
    body.classList.remove("scroll-up");
    body.classList.add("scroll-down");
  } else if (
    currentScroll < lastScroll &&
    body.classList.contains("scroll-down")
  ) {
    body.classList.remove("scroll-down");
    body.classList.add("scroll-up");
  }
  lastScroll = currentScroll;
});
*/
/*

});
*/
/*
function navBarUl() {
  var ul = document.getElementById("navbar");
  var li = document.createElement("li");
  li.appendChild(document.createTextNode("Showcase"));
  ul.appendChild(li);
}
*/
/*
const body = document.querySelector("body");
window.addEventListener("scroll", function () {
  alert("please diseable addblocker!");
  body.style.display = "none";
});*/
/*
const title = document.querySelector("h1");
const originalText = title.textContent;
const updatedText = "testing this out!";

title.addEventListener("mouseover", function () {
  title.textContent = updatedText;
});

title.addEventListener("mouseout", function () {
  title.textContent = originalText;
});
*/
/*
function navBarUl() {
  ul.innerHTML += `<li> test </li>`;
}
*/

/*
let form = document.forms.subform,
  elem = form.elements;
console.log(elem);

elem.submit.addEventListener("click", function (event) {
  alert("input value is: " + elem.subject.value);
});
form.addEventListener("submit", function (event) {
  event.preventDefault();

  if (!elem.subject.value) {
    alert("please input text");
    elem.subject.focus;
  }
  console.log("form submission succesful");
  alert("thanks for testing");
});
*/

/*
document.addEventListener("scroll", function () {
  navSections.forEach((individualSection, i) => {
    if (onScreen(individualSection)) {
      individualSection.className = "active";
    } else {
      individualSection.className = "";
    }
  });
});

*/
